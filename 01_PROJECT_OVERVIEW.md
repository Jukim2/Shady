# 프로젝트 개요

## 목표

하나의 2D 목표 그림자를 여러 종류의 알아보기 어려운 3D 형상으로 역생성하고, 게임에서 회전해 정답 그림자를 맞추게 한다.

## 입출력

입력은 흰 실루엣/검은 배경의 PNG와 YAML 레벨 설정이다. 출력은 OBJ 메시, 목표 그림자, 검증 그림자, 플레이어 시점 이미지, 정답 회전값, 품질 지표다.

## 주요 구성

- `src/shadow_factory.py`: 생성·연마·투영·검증·패키징
- `configs/`: 점검 및 양산 레벨 정의
- `assets/targets/`: 목표 실루엣
- `scripts/generate_sample_targets.py`: 외부 다운로드 없는 샘플 생성
- `tests/test_smoke.py`: 전체 공정 점검
- `output/`: 검증된 양산 결과

## 현재 범위

OBJ 생성과 레벨 데이터 출력까지 포함한다. Unity/Unreal에서의 입력 처리, 조각 피벗, UI, 저장 시스템은 엔진 통합 단계에서 구현한다.
